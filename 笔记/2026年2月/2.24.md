# 0. timeline
9:30 - 10:35 manaenv 环境配置
10:50 - 11:53 manaenv 架构分析


# 1.wallx接入
## 1.manaenv 环境配置
重装manaenv的环境 
```bash
source ./install.sh --conda manaenv -g -dp
```

## 2.manaenv 架构分析
cfg是config的简写，里面存了配置信息，是python的读取类，有点接口的感觉
yaml是本地保存配置的文件


# 2.Cngine 架构搭建
稀疏矩阵填充时，可以把索引的第一个位设置成标注位，用来记录该位置是在矩阵的左下还是右上位置。

首先将vert_connect进行一个scan操作，相当于将稀疏矩阵的offset定下来
遍历所有的弹簧，查找它在该offset中的偏移量

也许约束的生成应该写到布料里面而不是simulator里面

相同的约束可以放到一个约束类里面